# Backend environment example
PORT=4000
NODE_ENV=development

# Allowed frontend origins (comma separated)
FRONTEND_URL=http://localhost:3000,http://127.0.0.1:5500

# SMTP configuration (preferred)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Fallback Gmail credentials (if not using custom SMTP)
EMAIL_USER=
EMAIL_PASS=

# WordPress Headless CMS API configuration
# Local development: http://dfd-cms.local
# Staging: https://staging-cms.yourdomain.com
# Production: https://cms.yourdomain.com
WP_API_BASE_URL=http://dfd-cms.local

# API timeout in milliseconds (default: 10000)
WP_API_TIMEOUT=10000
